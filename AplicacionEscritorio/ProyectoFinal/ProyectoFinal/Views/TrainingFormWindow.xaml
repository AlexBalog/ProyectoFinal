<Window x:Class="ProyectoFinal.Views.TrainingFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:ProyectoFinal.ViewModels"
        xmlns:converters="clr-namespace:ProyectoFinal.Converters"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="850" Width="1000"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Convertidores -->
        <converters:BoolToTextConverter x:Key="BoolToTextConverter"/>
        <converters:Base64ToImageConverter x:Key="Base64ToImageConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Colores y estilos base -->
        <Color x:Key="PrimaryColor">#8A2BE2</Color>
        <Color x:Key="BackgroundColor">#121212</Color>
        <Color x:Key="SurfaceColor">#1E1E1E</Color>
        <Color x:Key="TextColorPrimary">#FFFFFF</Color>
        <Color x:Key="TextColorSecondary">#E0E0E0</Color>
        <Color x:Key="AccentColor">#333333</Color>
        <Color x:Key="SuccessColor">#4CAF50</Color>
        <Color x:Key="ErrorColor">#F44336</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="TextBrushPrimary" Color="{StaticResource TextColorPrimary}"/>
        <SolidColorBrush x:Key="TextBrushSecondary" Color="{StaticResource TextColorSecondary}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

        <!-- Estilo para labels -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Estilo para TextBox -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2A2A2A"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para ComboBox -->
        <Style x:Key="FormComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                  Background="{TemplateBinding Background}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  Focusable="False"
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z" 
                                              Fill="{StaticResource TextBrushSecondary}"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                      IsHitTestVisible="False"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                      Margin="12,0,25,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"/>

                            <Popup x:Name="Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">
                                <Grid MaxHeight="200" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border Background="{StaticResource SurfaceBrush}"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para ComboBoxItem -->
        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para CheckBox -->
        <Style x:Key="FormCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="CheckBoxBorder"
                                    Grid.Column="0"
                                    Width="20" Height="20"
                                    Background="{StaticResource SurfaceBrush}"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="2"
                                    CornerRadius="4"
                                    Margin="0,0,8,0">
                                <Path x:Name="CheckMark"
                                      Data="M 2 6 L 6 10 L 14 2"
                                      Stroke="{StaticResource PrimaryBrush}"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>

                            <ContentPresenter Grid.Column="1"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones principales -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#9C27B0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#666666"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RemoveButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para headers de sección -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Estilo para botones de ventana -->
        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushSecondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Ventana principal -->
    <Border CornerRadius="15" Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Content -->
                <RowDefinition Height="Auto"/>
                <!-- Actions -->
            </Grid.RowDefinitions>

            <!-- Header con título y botón cerrar -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="15,15,0,0" MouseLeftButtonDown="Header_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                        <TextBlock Text="💪" FontSize="16" Margin="0,0,10,0" Foreground="BlueViolet"/>
                        <TextBlock Text="{Binding WindowTitle}" 
                                   Foreground="{StaticResource TextBrushPrimary}" 
                                   FontWeight="SemiBold"/>
                    </StackPanel>

                    <Button x:Name="btnClose" 
                            Style="{StaticResource CloseButton}" 
                            Content="✕" 
                            HorizontalAlignment="Right" 
                            Margin="0,0,10,0"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Contenido principal -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30">
                <StackPanel MaxWidth="900">
                    <!-- Información básica -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="📋 Información Básica" Style="{StaticResource SectionHeader}"/>

                            <!-- Grid principal MODIFICADO para manejar la visibilidad del creador -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- ID (solo en modo edición) -->
                                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,15"
                                           Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="ID del Entrenamiento" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Id}" Style="{StaticResource FormTextBox}" IsEnabled="False"/>
                                </StackPanel>

                                <!-- Nombre -->
                                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Nombre *" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- Categoría -->
                                <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Categoría *" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedItem="{Binding Categoria}" 
                                              ItemsSource="{Binding CategoriasList}"
                                              Style="{StaticResource FormComboBox}"
                                              ItemContainerStyle="{StaticResource ComboBoxItemStyle}"/>
                                </StackPanel>

                                <!-- Músculo Principal -->
                                <StackPanel Grid.Column="4" Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Músculo Principal *" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedItem="{Binding MusculoPrincipal}" 
                                              ItemsSource="{Binding MusculosList}"
                                              Style="{StaticResource FormComboBox}"
                                              ItemContainerStyle="{StaticResource ComboBoxItemStyle}"/>
                                </StackPanel>

                                <!-- Duración -->
                                <StackPanel Grid.Column="0" Grid.Row="2" Margin="0,0,0,15">
                                    <TextBlock Text="Duración (minutos) *" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Duracion, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- Creador (solo en modo edición, readonly) -->
                                <StackPanel Grid.Column="2" Grid.Row="2" Margin="0,0,0,15"
                                           Visibility="{Binding IsCreadorVisible, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Creador" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Creador}" 
                                             Style="{StaticResource FormTextBox}" 
                                             IsEnabled="{Binding IsCreadorEditable}"/>
                                </StackPanel>

                                <!-- Likes (solo en modo edición) -->
                                <StackPanel Grid.Column="4" Grid.Row="2" Margin="0,0,0,15"
                                           Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="Likes" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Likes}" Style="{StaticResource FormTextBox}" IsEnabled="False"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Ejercicios -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="🏋️ Ejercicios del Entrenamiento" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Agregar ejercicio -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Seleccionar Ejercicio" Style="{StaticResource FieldLabel}"/>
                                    <Button Content="🔍 Buscar Ejercicio" 
                                            Style="{StaticResource PrimaryButton}"
                                            Height="40"
                                            Command="{Binding SelectEjercicioCommand}"/>
                                    <TextBlock Text="{Binding EjerciciosDetallados.Count, StringFormat='Ejercicios: {0}'}" 
                                               Style="{StaticResource FieldLabel}"
                                               Margin="0,10,0,0"
                                               Foreground="{StaticResource PrimaryBrush}"/>
                                </StackPanel>

                                <!-- Lista de ejercicios seleccionados -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Ejercicios Seleccionados" Style="{StaticResource FieldLabel}"/>
                                    <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding EjerciciosDetallados}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{StaticResource AccentBrush}"
                                                            BorderBrush="{StaticResource PrimaryBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="8"
                                                            Padding="12"
                                                            Margin="0,0,0,8">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <!-- Imagen del ejercicio -->
                                                            <Border Grid.Column="0" 
                                                                    Width="50" Height="50"
                                                                    Background="{StaticResource SurfaceBrush}"
                                                                    CornerRadius="6"
                                                                    Margin="0,0,12,0">
                                                                <Grid>
                                                                    <Image Source="{Binding foto, Converter={StaticResource Base64ToImageConverter}}"
                                                                           Stretch="UniformToFill">
                                                                        <Image.Clip>
                                                                            <RectangleGeometry RadiusX="4" RadiusY="4" 
                                                                                               Rect="0,0,50,50"/>
                                                                        </Image.Clip>
                                                                    </Image>

                                                                    <!-- Fallback cuando no hay imagen -->
                                                                    <TextBlock Text="🏋️" 
                                                                               FontSize="30" 
                                                                               HorizontalAlignment="Center"
                                                                               VerticalAlignment="Center"
                                                                               Foreground="{StaticResource TextBrushSecondary}">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                                <Style.Triggers>
                                                                                    <!-- Mostrar solo cuando la imagen no tiene source o falla al cargar -->
                                                                                    <DataTrigger Binding="{Binding foto}" Value="{x:Null}">
                                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding foto}" Value="">
                                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Grid>
                                                            </Border>

                                                            <!-- Información del ejercicio -->
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                                <TextBlock Text="{Binding nombre}" 
                                                                           FontWeight="SemiBold"
                                                                           FontSize="14"
                                                                           Foreground="{StaticResource TextBrushPrimary}"
                                                                           Margin="0,0,0,2"
                                                                           TextWrapping="Wrap"/>

                                                                <TextBlock Text="{Binding musculo, StringFormat='Músculo: {0}'}" 
                                                                           FontSize="11"
                                                                           Foreground="{StaticResource PrimaryBrush}"
                                                                           Margin="0,0,0,2"/>

                                                                <TextBlock Text="{Binding descripcion}" 
                                                                           FontSize="10"
                                                                           Foreground="{StaticResource TextBrushSecondary}"
                                                                           TextWrapping="Wrap"
                                                                           TextTrimming="None"/>
                                                            </StackPanel>

                                                            <!-- Botón eliminar -->
                                                            <Button Grid.Column="2"
                                                                    Content="🗑️"
                                                                    Style="{StaticResource RemoveButton}"
                                                                    Width="30" Height="30"
                                                                    FontSize="12"
                                                                    Padding="0"
                                                                    Command="{Binding DataContext.RemoveEjercicioCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Músculos -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="💪 Músculos Trabajados" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Agregar músculo -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Agregar Músculo" Style="{StaticResource FieldLabel}"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <ComboBox Grid.Column="0" 
                                                 SelectedItem="{Binding MusculoTemp}" 
                                                 ItemsSource="{Binding MusculosList}"
                                                 Style="{StaticResource FormComboBox}"
                                                 ItemContainerStyle="{StaticResource ComboBoxItemStyle}"
                                                 Margin="0,0,10,0"/>

                                        <Button Grid.Column="1" 
                                                Content="➕ Agregar" 
                                                Style="{StaticResource PrimaryButton}"
                                                Height="40"
                                                MinWidth="80"
                                                Command="{Binding AddMusculoCommand}"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Lista de músculos -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="{Binding Musculo.Count, StringFormat='Músculos ({0})'}" 
                                               Style="{StaticResource FieldLabel}"/>
                                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding Musculo}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="{StaticResource AccentBrush}"
                                                            BorderBrush="{StaticResource PrimaryBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="6"
                                                            Padding="10,8"
                                                            Margin="0,0,0,5">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Grid.Column="0" 
                                                                      Text="{Binding}" 
                                                                      VerticalAlignment="Center"
                                                                      Foreground="{StaticResource TextBrushPrimary}"/>

                                                            <Button Grid.Column="1"
                                                                    Content="✕"
                                                                    Style="{StaticResource RemoveButton}"
                                                                    Width="25" Height="25"
                                                                    FontSize="10"
                                                                    Padding="0"
                                                                    Command="{Binding DataContext.RemoveMusculoCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Estado y configuración -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="⚙️ Estado y Configuración" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Checkboxes de estado -->
                                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,15">
                                    <CheckBox Content="Entrenamiento aprobado" 
                                             IsChecked="{Binding Aprobado}"
                                             Style="{StaticResource FormCheckBox}"
                                             Margin="0,0,0,10"/>
                                    <CheckBox Content="Solicitud enviada" 
                                             IsChecked="{Binding Pedido}"
                                             Style="{StaticResource FormCheckBox}"/>
                                </StackPanel>

                                <!-- Motivo de rechazo -->
                                <StackPanel Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Margin="0,0,0,15">
                                    <TextBlock Text="Motivo de Rechazo" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding MotivoRechazo, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"
                                             Height="80"
                                             AcceptsReturn="True"
                                             TextWrapping="Wrap"
                                             VerticalContentAlignment="Top"
                                             Padding="12,8"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Imagen del entrenamiento -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="📷 Imagen del Entrenamiento" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Preview de la imagen -->
                                <Border Grid.Column="0" 
                                        Width="150" Height="150"
                                        Background="{StaticResource AccentBrush}"
                                        CornerRadius="10"
                                        BorderBrush="{StaticResource PrimaryBrush}"
                                        BorderThickness="2">
                                    <Grid>
                                        <!-- Imagen del entrenamiento -->
                                        <Image Source="{Binding Foto, Converter={StaticResource Base64ToImageConverter}}"
                                               Stretch="UniformToFill">
                                            <Image.Clip>
                                                <RectangleGeometry RadiusX="8" RadiusY="8" 
                                                                   Rect="0,0,150,150"/>
                                            </Image.Clip>
                                        </Image>

                                        <!-- Placeholder cuando no hay imagen -->
                                        <TextBlock Text="💪" 
                                                   FontSize="50" 
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Foreground="{StaticResource TextBrushSecondary}"/>

                                        <!-- Texto cuando no hay imagen -->
                                        <TextBlock Text="Sin imagen" 
                                                   FontSize="12"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Bottom"
                                                   Foreground="{StaticResource TextBrushSecondary}"
                                                   Margin="0,0,0,10"/>
                                    </Grid>
                                </Border>

                                <!-- Botones para manejar imagen -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="Selecciona una imagen representativa para el entrenamiento" 
                                               Style="{StaticResource FieldLabel}"
                                               Margin="0,0,0,15"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="📁 Seleccionar Imagen" 
                                                Style="{StaticResource PrimaryButton}"
                                                Height="35"
                                                MinWidth="150"
                                                Margin="0,0,15,0"
                                                Command="{Binding SelectImageCommand}"/>

                                        <Button Content="🗑️ Quitar Imagen" 
                                                Style="{StaticResource SecondaryButton}"
                                                Height="35"
                                                MinWidth="130"
                                                Command="{Binding RemoveImageCommand}"
                                                Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>

                                    <TextBlock Text="• Formatos: JPG, PNG, BMP, GIF" 
                                               FontSize="10"
                                               Foreground="{StaticResource TextBrushSecondary}"
                                               Margin="0,15,0,2"/>
                                    <TextBlock Text="• Tamaño máximo: 5MB" 
                                               FontSize="10"
                                               Foreground="{StaticResource TextBrushSecondary}"
                                               Margin="0,0,0,2"/>
                                    <TextBlock Text="• Se redimensionará automáticamente" 
                                               FontSize="10"
                                               Foreground="{StaticResource TextBrushSecondary}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Botones de acción -->
            <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="0,0,15,15" Padding="30,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Información adicional -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="💡 Los campos marcados con * son obligatorios" 
                                   Foreground="{StaticResource TextBrushSecondary}" 
                                   FontSize="12"/>
                        <TextBlock Text="Debe tener al menos un ejercicio para ser válido" 
                                   Foreground="{StaticResource TextBrushSecondary}" 
                                   FontSize="11"
                                   Margin="0,2,0,0"/>
                        <!-- NUEVO: Texto explicativo para el creador -->
                        <TextBlock Text="El creador se asigna automáticamente al crear nuevos entrenamientos" 
                                   Foreground="{StaticResource TextBrushSecondary}" 
                                   FontSize="11"
                                   Margin="0,2,0,0"
                                   Visibility="{Binding IsEditMode, Converter={StaticResource InverseBoolToVis}}"/>
                    </StackPanel>

                    <!-- Botones de acción -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="btnCancel" 
                                Content="Cancelar" 
                                Style="{StaticResource SecondaryButton}"
                                Height="45"
                                MinWidth="120"
                                Margin="0,0,15,0"
                                Command="{Binding CancelCommand}"/>

                        <Button x:Name="btnSave" 
                                Content="{Binding IsEditMode, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Actualizar|Crear'}"
                                Style="{StaticResource PrimaryButton}"
                                Height="45"
                                MinWidth="120"
                                Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Loading overlay -->
            <Border Grid.RowSpan="3" 
                    Background="#80000000" 
                    CornerRadius="15"
                    x:Name="LoadingOverlay"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" 
                                 Width="250" Height="6" 
                                 Foreground="{StaticResource PrimaryBrush}"
                                 Background="{StaticResource AccentBrush}"/>
                    <TextBlock Text="{Binding IsEditMode, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Actualizando entrenamiento...|Creando entrenamiento...'}"
                               Foreground="{StaticResource TextBrushPrimary}"
                               HorizontalAlignment="Center"
                               Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>