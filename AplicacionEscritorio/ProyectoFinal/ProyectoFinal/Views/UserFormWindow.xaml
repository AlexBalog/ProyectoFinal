<Window x:Class="ProyectoFinal.Views.UserFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:ProyectoFinal.ViewModels"
        xmlns:converters="clr-namespace:ProyectoFinal.Converters"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="750" Width="900"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Convertidores -->
        <converters:BoolToTextConverter x:Key="BoolToTextConverter"/>
        <converters:Base64ToImageConverter x:Key="Base64ToImageConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Colores y estilos base -->
        <Color x:Key="PrimaryColor">#8A2BE2</Color>
        <Color x:Key="BackgroundColor">#121212</Color>
        <Color x:Key="SurfaceColor">#1E1E1E</Color>
        <Color x:Key="TextColorPrimary">#FFFFFF</Color>
        <Color x:Key="TextColorSecondary">#E0E0E0</Color>
        <Color x:Key="AccentColor">#333333</Color>
        <Color x:Key="SuccessColor">#4CAF50</Color>
        <Color x:Key="ErrorColor">#F44336</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
        <SolidColorBrush x:Key="TextBrushPrimary" Color="{StaticResource TextColorPrimary}"/>
        <SolidColorBrush x:Key="TextBrushSecondary" Color="{StaticResource TextColorSecondary}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>

        <!-- Estilo para labels -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Estilo para error labels -->
        <Style x:Key="ErrorLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>

        <!-- Estilo para TextBox -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#2A2A2A"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para TextBox con error -->
        <Style x:Key="FormTextBoxError" TargetType="TextBox" BasedOn="{StaticResource FormTextBox}">
            <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <!-- Estilo para PasswordBox -->
        <Style x:Key="FormPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para ComboBox -->
        <Style x:Key="FormComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                  Background="{TemplateBinding Background}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  Focusable="False"
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z" 
                                              Fill="{StaticResource TextBrushSecondary}"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite"
                                      IsHitTestVisible="False"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                      Margin="12,0,25,0"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"/>

                            <Popup x:Name="Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">
                                <Grid MaxHeight="200" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border Background="{StaticResource SurfaceBrush}"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para ComboBoxItem -->
        <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para DatePicker CORREGIDO -->
        <Style x:Key="FormDatePicker" TargetType="DatePicker">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- TextBox para mostrar la fecha -->
                                <DatePickerTextBox x:Name="PART_TextBox" 
                                           Grid.Column="0"
                                           Background="Transparent"
                                           BorderThickness="0"
                                           Foreground="{TemplateBinding Foreground}"
                                           Focusable="{TemplateBinding Focusable}"
                                           HorizontalContentAlignment="Stretch"
                                           VerticalContentAlignment="Center"
                                           Margin="12,0,0,0"
                                           FontSize="{TemplateBinding FontSize}"/>

                                <!-- Botón para abrir el calendario -->
                                <Button x:Name="PART_Button"
                                Grid.Column="1"
                                Width="30"
                                Height="30"
                                Content="📅"
                                FontSize="14"
                                Margin="0,0,5,0"
                                Focusable="False"
                                Foreground="{StaticResource TextBrushSecondary}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="2">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#2A2A2A"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para DatePickerTextBox para quitar bordes blancos -->
        <Style TargetType="DatePickerTextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePickerTextBox">
                        <Grid>
                            <Border Background="Transparent"
                            BorderThickness="0"
                            Padding="0">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                      Background="Transparent"
                                      BorderThickness="0"
                                      IsTabStop="False"
                                      Margin="0"
                                      Padding="0"
                                      VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para CheckBox -->
        <Style x:Key="FormCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrushPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border x:Name="CheckBoxBorder"
                                    Grid.Column="0"
                                    Width="20" Height="20"
                                    Background="{StaticResource SurfaceBrush}"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="2"
                                    CornerRadius="4"
                                    Margin="0,0,8,0">
                                <Path x:Name="CheckMark"
                                      Data="M 2 6 L 6 10 L 14 2"
                                      Stroke="{StaticResource PrimaryBrush}"
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>
                            
                            <ContentPresenter Grid.Column="1"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones principales -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#9C27B0"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#666666"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para headers de sección -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Estilo para botones de ventana -->
        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrushSecondary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Ventana principal -->
    <Border CornerRadius="15" Background="{StaticResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Content -->
                <RowDefinition Height="Auto"/>
                <!-- Actions -->
            </Grid.RowDefinitions>

            <!-- Header con título y botón cerrar -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="15,15,0,0" MouseLeftButtonDown="Header_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                        <TextBlock Text="👤" FontSize="16" Margin="0,0,10,0" Foreground="BlueViolet"/>
                        <TextBlock Text="{Binding WindowTitle}" 
                                   Foreground="{StaticResource TextBrushPrimary}" 
                                   FontWeight="SemiBold"/>
                    </StackPanel>

                    <Button x:Name="btnClose" 
                            Style="{StaticResource CloseButton}" 
                            Content="✕" 
                            HorizontalAlignment="Right" 
                            Margin="0,0,10,0"
                            Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <!-- Contenido principal -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30">
                <StackPanel MaxWidth="800">
                    <!-- Información básica -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="📋 Información Básica" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- ID (solo en modo edición) -->
                                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,15"
                                           Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="ID de Usuario" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Id}" Style="{StaticResource FormTextBox}" IsEnabled="False"/>
                                </StackPanel>

                                <!-- Nombre -->
                                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Nombre *" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- Apellido -->
                                <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Apellido *" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Apellido, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- Email -->
                                <StackPanel Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" Margin="0,0,0,15">
                                    <TextBlock Text="Email *" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox" BasedOn="{StaticResource FormTextBox}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasEmailError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
                                                        <Setter Property="BorderThickness" Value="2"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                    <TextBlock Text="{Binding EmailError}" 
                                               Style="{StaticResource ErrorLabel}"
                                               Visibility="{Binding HasEmailError, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>

                                <!-- Fecha de Nacimiento -->
                                <StackPanel Grid.Column="0" Grid.Row="3" Margin="0,0,0,15">
                                    <TextBlock Text="Fecha de Nacimiento" Style="{StaticResource FieldLabel}"/>
                                    <DatePicker SelectedDate="{Binding FechaNacimiento}" 
                                               Style="{StaticResource FormDatePicker}"/>
                                </StackPanel>

                                <!-- Sexo -->
                                <StackPanel Grid.Column="2" Grid.Row="3" Margin="0,0,0,15">
                                    <TextBlock Text="Sexo" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedItem="{Binding Sexo}" 
                                              Style="{StaticResource FormComboBox}"
                                              ItemContainerStyle="{StaticResource ComboBoxItemStyle}">
                                        <ComboBox.Items>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Masculino</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Femenino</system:String>
                                        </ComboBox.Items>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Plan -->
                                <StackPanel Grid.Column="0" Grid.Row="4" Margin="0,0,0,15">
                                    <TextBlock Text="Plan" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedItem="{Binding Plan}" 
                                              Style="{StaticResource FormComboBox}"
                                              ItemContainerStyle="{StaticResource ComboBoxItemStyle}">
                                        <ComboBox.Items>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Gratuito</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Premium</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Admin</system:String>
                                        </ComboBox.Items>
                                    </ComboBox>
                                </StackPanel>

                                <!-- Formulario completado -->
                                <StackPanel Grid.Column="2" Grid.Row="4" Margin="0,0,0,15" VerticalAlignment="Center">
                                    <TextBlock Text="Estado" Style="{StaticResource FieldLabel}"/>
                                    <CheckBox Content="Formulario completado" 
                                             IsChecked="{Binding Formulario}"
                                             Style="{StaticResource FormCheckBox}"
                                             Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Contraseña (solo en modo creación) -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20"
                            Visibility="{Binding ShowPasswordFields, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="🔒 Contraseña" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Contraseña -->
                                <StackPanel Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Contraseña * (mínimo 6 caracteres)" Style="{StaticResource FieldLabel}"/>
                                    <PasswordBox x:Name="txtPassword" Style="{StaticResource FormPasswordBox}"
                                                PasswordChanged="TxtPassword_PasswordChanged"/>
                                </StackPanel>

                                <!-- Confirmar contraseña -->
                                <StackPanel Grid.Column="2" Margin="0,0,0,15">
                                    <TextBlock Text="Confirmar Contraseña *" Style="{StaticResource FieldLabel}"/>
                                    <PasswordBox x:Name="txtConfirmPassword" Style="{StaticResource FormPasswordBox}"
                                                PasswordChanged="TxtConfirmPassword_PasswordChanged"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Cambiar contraseña (solo en modo edición) -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20"
                            Visibility="{Binding ShowPasswordChangeOption, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="🔒 Cambiar Contraseña" Style="{StaticResource SectionHeader}"/>
                            <TextBlock Text="Deja en blanco para mantener la contraseña actual" 
                                       Style="{StaticResource FieldLabel}"
                                       FontStyle="Italic" Margin="0,0,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Nueva contraseña -->
                                <StackPanel Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Nueva Contraseña (opcional)" Style="{StaticResource FieldLabel}"/>
                                    <PasswordBox x:Name="txtNewPassword" Style="{StaticResource FormPasswordBox}"
                                                PasswordChanged="TxtNewPassword_PasswordChanged"/>
                                </StackPanel>

                                <!-- Confirmar nueva contraseña -->
                                <StackPanel Grid.Column="2" Margin="0,0,0,15">
                                    <TextBlock Text="Confirmar Nueva Contraseña" Style="{StaticResource FieldLabel}"/>
                                    <PasswordBox x:Name="txtConfirmNewPassword" Style="{StaticResource FormPasswordBox}"
                                                PasswordChanged="TxtConfirmNewPassword_PasswordChanged"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Datos físicos -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="📏 Datos Físicos" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Altura -->
                                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,15">
                                    <TextBlock Text="Altura (cm)" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Altura, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- Peso -->
                                <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,15">
                                    <TextBlock Text="Peso (kg)" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding Peso, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- IMC (calculado automáticamente) -->
                                <StackPanel Grid.Column="4" Grid.Row="0" Margin="0,0,0,15">
                                    <TextBlock Text="IMC (calculado)" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding IMC, StringFormat=F2}" 
                                             Style="{StaticResource FormTextBox}" IsEnabled="False"/>
                                </StackPanel>

                                <!-- Nivel de Actividad -->
                                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,15">
                                    <TextBlock Text="Nivel de Actividad" Style="{StaticResource FieldLabel}"/>
                                    <ComboBox SelectedItem="{Binding NivelActividad}" 
                                              Style="{StaticResource FormComboBox}"
                                              ItemContainerStyle="{StaticResource ComboBoxItemStyle}">
                                        <ComboBox.Items>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Sedentario</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Ligero</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Moderado</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Activo</system:String>
                                            <system:String xmlns:system="clr-namespace:System;assembly=mscorlib">Muy Activo</system:String>
                                        </ComboBox.Items>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Objetivos -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="🎯 Objetivos" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Objetivo de Peso -->
                                <StackPanel Grid.Column="0" Margin="0,0,0,15">
                                    <TextBlock Text="Objetivo de Peso (kg)" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding ObjetivoPeso, UpdateSourceTrigger=PropertyChanged}" 
                                             Style="{StaticResource FormTextBox}"/>
                                </StackPanel>

                                <!-- Objetivo de Tiempo -->
                                <StackPanel Grid.Column="2" Margin="0,0,0,15">
                                    <TextBlock Text="Objetivo de Tiempo (semanas)" Style="{StaticResource FieldLabel}"/>
                                    <TextBox Text="{Binding ObjetivoTiempo, UpdateSourceTrigger=PropertyChanged}" 
                                            Style="{StaticResource FormTextBox}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Seleccionar imagen -->
                    <Border Background="{StaticResource SurfaceBrush}" 
                            CornerRadius="15" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="📷 Imagen de Perfil" Style="{StaticResource SectionHeader}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Preview de la imagen -->
                                <Border Grid.Column="0" 
                    Width="120" Height="120"
                    Background="{StaticResource AccentBrush}"
                    CornerRadius="10"
                    BorderBrush="{StaticResource PrimaryBrush}"
                    BorderThickness="2">
                                    <Grid>
                                        <!-- Imagen del usuario -->
                                        <Image Source="{Binding Foto, Converter={StaticResource Base64ToImageConverter}}"
                           Stretch="UniformToFill"
                           Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}}">
                                            <Image.Clip>
                                                <RectangleGeometry RadiusX="8" RadiusY="8" 
                                               Rect="0,0,120,120"/>
                                            </Image.Clip>
                                        </Image>

                                        <!-- Placeholder cuando no hay imagen -->
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                Visibility="{Binding HasImage, Converter={StaticResource InverseBoolToVis}, ConverterParameter=Inverse}">
                                            <TextBlock Text="👤" 
                                   FontSize="40" 
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource TextBrushSecondary}"/>
                                            <TextBlock Text="Sin imagen" 
                                   FontSize="10"
                                   HorizontalAlignment="Center"
                                   Foreground="{StaticResource TextBrushSecondary}"
                                   Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Botones para manejar imagen -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="Selecciona una imagen de perfil para el usuario" 
                           Style="{StaticResource FieldLabel}"
                           Margin="0,0,0,15"/>

                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="📁 Seleccionar Imagen" 
                            Style="{StaticResource PrimaryButton}"
                            Height="35"
                            MinWidth="140"
                            Margin="0,0,10,0"
                            Command="{Binding SelectImageCommand}"/>

                                        <Button Content="🗑️ Quitar Imagen" 
                            Style="{StaticResource SecondaryButton}"
                            Height="35"
                            MinWidth="120"
                            Command="{Binding RemoveImageCommand}"
                            Visibility="{Binding HasImage, Converter={StaticResource BoolToVis}}"/>
                                    </StackPanel>

                                    <TextBlock Text="• Formatos: JPG, PNG, BMP, GIF" 
                           FontSize="10"
                           Foreground="{StaticResource TextBrushSecondary}"
                           Margin="0,10,0,2"/>
                                    <TextBlock Text="• Tamaño máximo: 5MB" 
                           FontSize="10"
                           Foreground="{StaticResource TextBrushSecondary}"
                           Margin="0,0,0,2"/>
                                    <TextBlock Text="• Se redimensionará automáticamente" 
                           FontSize="10"
                           Foreground="{StaticResource TextBrushSecondary}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Botones de acción -->
            <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                    CornerRadius="0,0,15,15" Padding="30,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Información adicional -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="💡 Los campos marcados con * son obligatorios" 
                                   Foreground="{StaticResource TextBrushSecondary}" 
                                   FontSize="12"/>
                        <TextBlock Text="La contraseña debe tener al menos 6 caracteres" 
                                   Foreground="{StaticResource TextBrushSecondary}" 
                                   FontSize="11"
                                   Margin="0,2,0,0"
                                   Visibility="{Binding ShowPasswordFields, Converter={StaticResource BoolToVis}}"/>
                    </StackPanel>

                    <!-- Botones de acción -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="btnCancel" 
                                Content="Cancelar" 
                                Style="{StaticResource SecondaryButton}"
                                Margin="0,0,15,0"
                                Command="{Binding CancelCommand}"/>

                        <Button x:Name="btnSave" 
                                Content="{Binding IsEditMode, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Actualizar|Crear'}"
                                Style="{StaticResource PrimaryButton}"
                                Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Loading overlay -->
            <Border Grid.RowSpan="3" 
                    Background="#80000000" 
                    CornerRadius="15"
                    x:Name="LoadingOverlay"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" 
                                 Width="200" Height="6" 
                                 Foreground="{StaticResource PrimaryBrush}"
                                 Background="{StaticResource AccentBrush}"/>
                    <TextBlock Text="{Binding IsEditMode, Converter={StaticResource BoolToTextConverter}, ConverterParameter='Actualizando usuario...|Creando usuario...'}"
                               Foreground="{StaticResource TextBrushPrimary}"
                               HorizontalAlignment="Center"
                               Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>